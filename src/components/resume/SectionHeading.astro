---
interface Props {
	as?: 'h2' | 'h3';
}

const { as: Tag = 'h2' } = Astro.props;
---

<Tag
	class="font-display mb-6 text-2xl font-semibold tracking-tight text-zinc-900 dark:text-zinc-100 sm:mb-7 sm:text-3xl"
>
	<slot />
</Tag>
