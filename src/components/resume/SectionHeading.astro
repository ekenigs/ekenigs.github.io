---
interface Props {
	as?: 'h2' | 'h3';
}

const { as: Tag = 'h2' } = Astro.props;
---

<Tag
	class="font-display mb-8 border-b border-zinc-200 pb-3 text-2xl font-semibold tracking-tight text-zinc-900 dark:border-zinc-800 dark:text-zinc-100 sm:text-3xl"
>
	<slot />
</Tag>
