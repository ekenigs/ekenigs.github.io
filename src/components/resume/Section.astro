---
interface Props {
	id?: string;
	class?: string;
}

const { id, class: className = '' } = Astro.props;
---

<section
	id={id}
	class:list={[
		'mx-auto w-full max-w-3xl px-4 sm:px-6 lg:px-8 py-8 sm:py-10 lg:py-12',
		className,
	]}
>
	<slot />
</section>
